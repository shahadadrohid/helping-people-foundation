import React from 'react';

const SocialLogin = () => {
    return (
        <div>
            <p>OR</p>

            <button className='border-0 btn btn-primary w-50 mx-auto py-2 rounded-pill'>Sign in with Google</button>
        </div>
    );
};

export default SocialLogin;